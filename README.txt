ИНСТРУКЦИЯ:

1. Установи зависимости:
   pip install -r requirements.txt

2. Заполни .env:
   OPENROUTER_API_KEY=... (получить на https://openrouter.ai/keys)
   TELEGRAM_TOKEN=... (от @BotFather)
   CHAT_ID=... (узнать через @userinfobot)

3. Запусти скрипт:
   python main.py
